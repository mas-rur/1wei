export default function CreatePage() {
  return (
    <section className="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
      <div className="rounded-2xl border border-border bg-abyss-raised p-12 text-center">
        <p className="font-mono text-xs uppercase tracking-wider text-base-blue">Phase 3</p>
        <h1 className="mt-3 font-display text-2xl font-semibold">Creating is next</h1>
        <p className="mx-auto mt-2 max-w-sm text-sm text-mist">
          Deploying a collection and minting — normal or lazy — land here once the trading UI is wired up.
        </p>
      </div>
    </section>
  );
}
